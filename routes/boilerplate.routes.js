const express = require('express');
const router = express.Router();

//model
const Item = require('../models/boilerplate.model');

//routes
router.get('/', (req,res)=>{
    //Item.find()
})

module.exports = router;